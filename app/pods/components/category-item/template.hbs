<div class="row">
    <div class="col-md-7">
      {{#link-to 'recepies.list' item.id}}
      <img src="{{item.imgLink}}" class="img-responsive" alt="">
      {{/link-to}}
    </div>
    <div class="col-md-5">
        <h3>{{item.name}}</h3>
        <p>{{item.description}}</p>
    </div>
    <div class="panel-footer text-right">
      {{yield}}
    </div>
</div>
